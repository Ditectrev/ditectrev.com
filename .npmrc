# Angular CLI and tooling expect a flat node_modules; pnpm uses a strict layout by default.
shamefully-hoist=true

# Use same peer dependency resolution as before (legacy-peer-deps equivalent).
auto-install-peers=true
